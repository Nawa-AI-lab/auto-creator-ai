# AutoCreator AI - Minimal Requirements for Render (Free Tier)
# ==========================================================
# هذه الحزم لا تحتاج Rust compiler - تعمل مباشرة

# === Web Framework ===
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# === HTTP Client ===
httpx>=0.26.0
aiofiles>=23.2.0

# === Configuration ===
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# === Database (SQLite for free tier) ===
sqlalchemy>=2.0.0
aiosqlite>=0.19.0  # SQLite async (بدون Rust)

# === Redis (Key-Value Store) ===
redis>=5.0.0

# === Background Tasks ===
celery>=5.3.0

# === AI Services (Cloud-based - لا تحتاج Rust) ===
# باستخدام HTTP API بدلاً من المكتبات المحلية

# 1. Groq API (Llama 2 - مجاني)
groq>=0.4.0

# 2. Hugging Face API (مجاني)
huggingface_hub>=0.20.0

# 3. Google Gemini API (مجاني)
google-generativeai>=0.3.0

# === Video Processing (FFmpeg فقط - بدون مكتبات Python) ===
# FFmpeg يجب تثبيته على Render من الإعدادات

# === Development Tools ===
pytest>=7.4.0
pytest-asyncio>=0.23.0

# === المتطلبات الاختيائية (إذا فشل التثبيت، يمكن تجاهلها) ===
# edge-tts>=6.1.0    #语音合成
# TTS>=0.22.0        # Coqui TTS (يتطلب Rust)
